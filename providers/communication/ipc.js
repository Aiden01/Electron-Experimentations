const path = require('path')
const M = require('./Manager')
const Manager = new M()
/**
     * register the handlers
     * Example:
     * Manager.addHandler('name', callback-function)
     */

/**
 * Load the handlers
 */
Manager.load()